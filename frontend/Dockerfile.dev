FROM node:20-alpine

WORKDIR /app

# Copy only package manifests for dependency caching
# Source code is mounted at runtime via volume mount
COPY package*.json ./
RUN npm install

EXPOSE 5173

# Start Vite dev server listening on all network interfaces
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0", "--port", "5173"]
